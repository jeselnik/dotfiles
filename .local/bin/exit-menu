#!/bin/sh

. bemenu-opts

opts="\
Logout
Shutdown
Reboot\
"

choice=$(printf "%s" "$opts" | bemenu -l 3 -p "😂👌💯" -w --no-exec)

case "$choice" in
  Logout) swaymsg exit & ;;
  Shutdown) systemctl poweroff & ;;
  Reboot) systemctl reboot & ;;
esac
